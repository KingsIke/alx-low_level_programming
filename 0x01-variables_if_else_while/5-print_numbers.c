#include <stdio.h>

/**
 *Description: main - Prints out a string to stdout.
 *
 * Return: Always 0 (Success)
 */
 
int main(void)
{
int n;
while (n < 10)
{
printf("%d", n);
n += 1;
}
printf("\n");
return (0);
}
